//sort() => sorts the element of an array in place and returns the sorted array. the default sort order
// is built upon  converting the elements into strings and the comparing their sequnece of UTF-16 code untis values.

const fruits = ["banana", "apple","orange","grape"];
fruits.sort()
console.log(fruits);
